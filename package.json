{
  "name": "puppeteer-vercel-example",
  "version": "1.0.0",
  "description": "A simple Puppeteer example running on Vercel",
  "main": "index.js",
  "scripts": {
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/node": "^24.0.0",
    "puppeteer": "^20.0.0"  // Ensure this is correctly specified
  },
  "engines": {
    "node": ">=14.17.0"
  }
}
